package com.example.snapget.core.model

/**
 * Che do giao dien app (muc Theme trong Settings).
 * DARK la mac dinh (DESIGN.md — app thiet ke dark-first); SYSTEM theo cai dat may.
 */
enum class ThemeMode {
    DARK,
    LIGHT,
    SYSTEM,
}

package com.example.snapget.core.constants

/**
 * Cloud Firestore collection names (Firestore has no database/collection ids,
 * so we only need the collection paths here).
 */
object FirestoreConfig {
    const val USERS = "users"
    const val NOTIFICATIONS = "notifications"
    const val MESSAGES = "messages"
    const val FRIENDSHIPS = "friendships"
    const val POSTS = "posts"
}
